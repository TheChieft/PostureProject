# PostureProject — Python dependencies
# Install with: pip install -r requirements.txt
#
# Tested on Python 3.11 x64 / Windows 11 ARM (Snapdragon X)
# Tkinter is bundled with the official CPython Windows installer.

opencv-python>=4.9.0
mediapipe>=0.10.9
numpy>=1.26.0
psutil>=5.9.0     # optional CPU monitoring
