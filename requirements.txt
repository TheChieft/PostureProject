# PostureProject — Python dependencies
# Install with: py -3.11 -m pip install --only-binary :all: -r requirements.txt
#
# Tested on Python 3.11 ARM64 / Windows 11 ARM (Snapdragon X)
# Tkinter is bundled with the official CPython Windows installer.
# --only-binary :all: forces pre-built wheels only (avoids compile errors on Windows ARM64)

opencv-python>=4.9.0
mediapipe==0.10.30
numpy>=1.26.0,<2.0
psutil>=5.9.0
